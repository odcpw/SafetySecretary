# Synthetic SDS Fixtures

These safety data sheet fixtures are fake. They do not describe real
products, suppliers, incidents, companies, workplaces, photos, or measured
exposures.

The fixtures exist to test SDS extraction shape and review gating with a
deterministic mocked LLM response. They intentionally use plain text so the
test source is auditable and does not require PDF or DOCX parsing.
